# Portafolio Mariana

Portafolio personal construido con Next.js, React, Tailwind CSS y Framer Motion.

## Comandos

```bash
npm install
npm run dev
npm run lint
npm run build
npm run start
```

## Despliegue

Proyecto listo para desplegar en Vercel:

- Framework: Next.js
- Build command: `npm run build`
- Install command: `npm install`
- Output: automático de Next.js

No subas `node_modules`, `.next`, `.next-build` ni `.next-dev`; ya están ignorados en `.gitignore`.
